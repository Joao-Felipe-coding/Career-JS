// for (let i = 0; i < 10; i++) {
//   const numeroE = i % 2 === 0 ? 'par' : 'impar';
//   console.log(`${i} ${numeroE}`);
//   }

const frutas = ['maçã', 'uva', 'pêra', 'melancia'];

for (let i = 0; i < frutas.length; i++) {
  console.log(`fruta${i+1} - ${frutas[i]}`);
}
