const nomes = ['João Felipe', 'Paulo', 'Fernando'];

// for (let valor = 0; valor < nome.length; valor++) {
//   const element = nome[valor];
//   console.log(element);
// }

//Melhor forma

for (const valor of nomes) {
  console.log(valor);
}
